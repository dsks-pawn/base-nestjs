export enum UserStatus {
  INACTIVE,
  ACTIVE,
}

export enum UserRole {
  USER = 1,
  ADMIN = 2,
  SUPER_ADMIN = 3,
}

export enum UserVerifyEmail {
  NO,
  YES,
}

export enum UserVerifyPhone {
  NO,
  YES,
}
